# Arrow CRM Database Configuration
# Copy to .env and adjust if needed (defaults work for local dev)

CRM2_DB_HOST=localhost
CRM2_DB_PORT=5432
CRM2_DB_NAME=crm2_development
CRM2_DB_TEST_NAME=crm2_test
CRM2_DB_USER=crm2_user
CRM2_DB_PASSWORD=crm2_password

# ============================================
# AWS S3 Configuration (Required for Production)
# ============================================
# These are required when using ActiveStorage with S3 in production.
# In development, files are stored locally on disk.

# Your AWS access key ID
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE

# Your AWS secret access key
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# AWS region where your S3 bucket is located
AWS_REGION=us-east-1

# S3 bucket name for document storage
AWS_BUCKET=arrow-crm-documents-production
